import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['example.com', 'image.freepik.com'], // Add your allowed image domains here
  },
};

export default nextConfig;
